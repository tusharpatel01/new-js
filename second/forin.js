//for inn loop mainly use in objects

const obj={
    rb: 'ruby',
    cpp : 'c++',
    js : 'javascript'
}
for (const key in obj) {
   // console.log(key);
    //rb
    // cpp
    // js
    
  //  console.log(obj[key]);
  //console.log(`key and value of object are ${key} and ${obj[key]}`);
  
    
}
//use of for in loop in arrays 
const names=["tushar","rahsut","saurabh","shersa"]
for (const name in names) {
    // console.log(names[name]);
    //console.log(`key vlaue pairs are ${name} and ${names[name]}`);
    
    
    
}